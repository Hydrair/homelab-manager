info:
  name: servers
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/servers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
